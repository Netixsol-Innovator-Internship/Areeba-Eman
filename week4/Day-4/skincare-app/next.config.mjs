/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["makeup-api.herokuapp.com", "d3t32hsnjxo7q6.cloudfront.net"], // add the actual image domains
  },
};

export default nextConfig;